"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.processResultText = processResultText;
function processResultText(text) {
    const extractValue = (regex, txt) => regex.exec(txt)?.groups?.['value'];
    const amount = [
        extractValue(/ETB(?<value>[\d,]+\.\d+)/, text),
        extractValue(/Amount (?<value>[\d,]+\.\d+) ETB/, text)
    ].find(it => it != undefined);
    const payer = extractValue(/Payer (?<value>[\w ]+)/, text);
    const payerAccount = extractValue(/Payer [\w ]+\s*Account (?<value>1\*+\d{3})/, text);
    const receiver = extractValue(/Receiver (?<value>[\w ]+)/, text);
    const receiverAccount = extractValue(/Receiver [\w ]+\s*Account (?<value>1\*+\d{3})/, text);
    const paymentDate = extractValue(/Payment Date \w+ (?<value>[\w ]+)/, text);
    const reference = extractValue(/Reference No\. (?<value>FT\w{10})/, text);
    const reason = extractValue(/Reason (?<value>\w*)/, text);
    return {
        fullText: text,
        amount: amount ? Number.parseFloat(amount.replace(",", "")) : undefined,
        payer,
        receiver,
        reference,
        payerAccount,
        receiverAccount,
        reason,
        date: paymentDate,
    };
}
//# sourceMappingURL=process-text.js.map