<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Adama Shopping Post</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="/css/cpeh.css">
  <style>

  </style>
</head>
<body>

<nav class="navbar">
  <a href="/" class="navbar-brand"> <img src="images/ads.png" width="95" height="95" alt="Adama Shopping Logo"> Adama Shopping</a>
  <ul class="navbar-nav">
    <li class="nav-item"><a href="/" class="nav-link">Home</a></li>
    <li class="nav-item"><a href="/about" class="nav-link">About</a></li>
    <li class="nav-item"><a href="/privecy" class="nav-link">Privacy</a></li>
    <li class="nav-item"><a href="/services" class="nav-link">Service</a></li>
  </ul>
</nav>

<h2><i class="fas fa-plus-circle"></i> Create New Post</h2>

<div class="form-container">
  <% if (user) { %>
    <% if (user.is_verified) { %>
      <form action="/post" method="POST" enctype="multipart/form-data">
        <div class="form-group">
            <i class="fas fa-pen"></i>
            <input type="text" name="title" placeholder="Title" required>
        </div>
        <div class="form-group">
            <i class="fas fa-align-left"></i>
            <textarea name="description" placeholder="Description" rows="3" required></textarea>
        </div>
        <div class="form-group">
            <i class="fas fa-money-bill-wave"></i> <!-- Changed bugget-sign to money-bill-wave for clarity -->
            <input type="number" name="budget" placeholder="(Birr) Budget" min="1" step="0.01" required>
        </div>
        <div class="form-group">
            <i class="fas fa-tags"></i>
            <select name="category" required>
                <option value="">-- Select Category --</option>
                <option value="mebrat">mebrat</option>
                <option value="17">17</option>
                <option value="arada">Arada</option>
            </select>
        </div>

        <!-- 🏦 Bank Account Number (NEW FIELD) -->
        <div class="form-group">
          <i class="fas fa-university"></i>
          <input 
            type="text" 
            name="account_number" 
            placeholder="Bank Account Number (13–14 digits)" 
            pattern="^[0-9]{13,14}$" 
            title="Enter a valid 13 or 14-digit account number"
            required>
        </div>

        <div class="form-group">
            <!-- Custom File Upload -->
            <label class="custom-file-upload" for="imageUpload">
                <span class="file-upload-button">
                    <i class="fas fa-cloud-upload-alt"></i> Upload Image
                </span>
                <span class="file-upload-text" id="fileName">No file chosen</span>
                <input type="file" name="image" id="imageUpload" accept="image/*">
            </label>
        </div>
        <button type="submit"><i class="fas fa-paper-plane"></i> Post Delivery</button>
      </form>
    <% } else { %>
      <div class="warning">⚠️ You are not verified yet. Wait for their approval to post.</div>
      <form>
        <div class="form-group"><i class="fas fa-pen"></i><input type="text" placeholder="Title" disabled></div>
        <div class="form-group"><i class="fas fa-align-left"></i><textarea placeholder="Description" disabled></textarea></div>
        <div class="form-group"><i class="fas fa-money-bill-wave"></i><input type="number" placeholder="Budget (Birr)" disabled></div>
        <div class="form-group"><i class="fas fa-tags"></i><select disabled><option>-- Select Category --</option></select></div>
        <div class="form-group"><i class="fas fa-university"></i><input type="text" placeholder="Bank Account Number (13–14 digits)" disabled></div>
        <div class="form-group">
            <label class="custom-file-upload disabled">
                <span class="file-upload-button">
                    <i class="fas fa-cloud-upload-alt"></i> Upload Image
                </span>
                <span class="file-upload-text">Disabled</span>
                <input type="file" disabled>
            </label>
        </div>
        <button type="submit" disabled><i class="fas fa-ban"></i> Create Post</button>
      </form>
    <% } %>
  <% } else { %>
    <div class="warning">⚠️ Please <a href="/login">login</a> to create a post.</div>
  <% } %>
</div>

<h2 style="text-align:center;"><i class="fas fa-clipboard-list"></i> All Active Posts</h2>

<div class="post-container">
  <% if (posts && posts.length > 0) { %>
    <% posts.forEach(post => { %>
      <div class="post">
        <% if (post.image) { %>
          <img class="post-image" src="/images/<%= post.image %>" alt="Post image">
        <% } else { %>
          <img class="post-image" src="/images/default.png" alt="Post image">
        <% } %>

        <h3><%= post.title %></h3>
        <p><%= post.description %></p>
        <p class="price"><i class="fas fa-hand-holding"></i> <%= post.budget %> Birr</p>
        <p><em><i class="fas fa-tag"></i> Category: <%= post.category %></em></p>
      
        <div class="user-info">
          <% if (post.user_image) { %>
            <img src="/images/<%= post.user_image %>" alt="User">
          <% } else { %>
            <img src="/images/default-user.png" alt="User">
          <% } %>
          <div>
            <strong><%= post.user_name %></strong><br>
            <% if (post.is_verified) { %>
              <span class="verified"><i class="fas fa-check-circle"></i> Verified</span>
            <% } %>
          </div>
        </div>
      </div>
    <% }) %>
  <% } else { %>
    <p style="text-align:center; color: var(--light-text); font-size: 18px; margin-top: 50px;">
        <i class="fas fa-inbox" style="font-size: 48px; display: block; margin-bottom: 10px;"></i>
        No posts yet. Be the first to create one!
    </p>
  <% } %>
</div>

<a href="/" class="back-link"><i class="fas fa-arrow-circle-left"></i> Back to Home</a>

 <script src="/js/cp.js"></script>

</body>
</html>